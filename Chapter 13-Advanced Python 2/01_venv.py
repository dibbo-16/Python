# pip install virtualenv
# virtualenv env # env is a environment name. we can take any name
# we are doing above things in the terminal
# here env is a python isolated environment. Now we can take any version in env
# pip install panda== 1.5.2  write in cmd at chapter-13- Advanced Python 1 folder
# then in cmd .\env\Scripts\activate.ps1. it is for activate env
# pip freeze (it wil help to show all packages which are installed)
# pip freeze > requirements.txt
# pip install -r .\requirements.txt # requirements er all package install hoye jabe any virtual enviromnent ah